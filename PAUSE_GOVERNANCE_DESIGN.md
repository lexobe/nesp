# 不可升级但可安全暂停：治理与安全阀设计草案

## 目标
- 不破坏 **trustless + 无仲裁** 的结算逻辑
- 允许在重大漏洞时 **暂停新增风险**
- 治理动作透明、可监督、可审计

## 设计原则
1. **不影响既有订单结算**（已进入状态机的订单不被改写）
2. **只暂停新增风险**（新订单 / 新托管资金）
3. **治理透明**（多签 + 时间锁 + 事件记录）

## 方案
### 1) Pause Scope（暂停范围）
- 允许暂停：`createOrder` / `acceptOrder` / `fundEscrow`（新增托管）
- 不允许暂停：`settle` / `withdraw` / `timeout`（既有订单结算路径）

### 2) Governance
- **多签**：Gnosis Safe（m-of-n）
- **时间锁**：至少 24–48h
- **两步转移**：propose → accept

### 3) 事件与可见性
- 所有治理操作必须 emit event
- 前端与监控系统实时提示（暂停中）

## 风险与权衡
- 牺牲“绝对不可变”换取“安全可控”
- 通过限制暂停范围，避免演化为事实仲裁

## 可选替代方案（更硬核）
- **无暂停**：完全依赖不可升级 + 多重审计 + 公开测试
- **只读冻结**：仅暂停新增订单，不影响任何结算与提款

